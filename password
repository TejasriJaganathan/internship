import random
import string
passkeys=string.ascii_letters+string.digits
while True:
 length=int(input("Enter password length:"))
 print("The password length is:",length)
 password= ''.join(random.choice(passkeys) for _ in range(length))
 print("The generated password for the provided length is:",password)
